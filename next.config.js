/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['leonidasesteban.com', 'raw.githubusercontent.com'],
  },
}

module.exports = nextConfig
